# Lesson 5: ORDER BY Sorting

## Overview
ORDER BY sorts results.

### MySQL Example
```sql
SELECT first_name, salary
FROM employees
ORDER BY salary DESC;
```

### SQLite Example
```sql
SELECT first_name, salary
FROM employees
ORDER BY salary DESC;
```

### Try It Yourself
Sort employees by last_name.
