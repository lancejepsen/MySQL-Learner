# Lesson 8: COUNT() Aggregation

## Overview
COUNT() returns the number of rows.

### MySQL Example
```sql
SELECT COUNT(*) AS total_employees
FROM employees;
```

### SQLite Example
```sql
SELECT COUNT(*) AS total_employees
FROM employees;
```

### Try It Yourself
Count how many departments exist.
